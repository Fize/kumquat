Kumquat Engine Manager has been deployed!

IMPORTANT: CRD Installation Notes
---------------------------------
The following CRDs should be installed BEFORE deploying this chart:
  - applications.apps.kumquat.io
  - managedclusters.storage.kumquat.io
  - workspaces.workspace.kumquat.io

The following CRDs should NOT be installed (they conflict with the aggregated APIService):
  - clusters.cluster.kumquat.io (virtual Cluster API is provided by the aggregated APIServer)

Install CRDs:
  kubectl apply -f config/crd/bases/apps.kumquat.io_applications.yaml
  kubectl apply -f config/crd/bases/storage.kumquat.io_managedclusters.yaml
  kubectl apply -f config/crd/bases/workspace.kumquat.io_workspaces.yaml

Getting Started
---------------
Get information about the deployed Clusters (via aggregated API):
  kubectl get clusters.cluster.kumquat.io

Get information about the deployed ManagedClusters (backend storage):
  kubectl get managedclusters.storage.kumquat.io

Get information about the deployed Workspaces:
  kubectl get workspaces.workspace.kumquat.io

Get information about the deployed Applications:
  kubectl get applications.apps.kumquat.io [--namespace <namespace>]

Verify APIService is working:
  kubectl get apiservice v1alpha1.cluster.kumquat.io
  # Should show: SERVICE = {{ .Release.Namespace }}/{{ include "manager.fullname" . }}-webhook

For more information on running Kumquat Engine, visit:
https://github.com/fize/kumquat/engine