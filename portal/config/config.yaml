server:
  bindAddr: ":8080"
  metrics:
    enabled: false
  trace:
    enabled: false

sql:
  type: sqlite3
  host: "./portal.db"
  user: ""
  password: ""
  db: ""
  maxIdleConns: 10
  maxOpenConns: 100
  debug: false

log:
  level: info
  format: json

jwt:
  secret: "change-this-secret-in-production"
  expire_duration: 24h
  reset_expire_duration: 10m

security:
  allowed_email_domains: []
